import React from 'react'
import ReactDOM from 'react-dom/client'
import ResultSummary from './App/app'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ResultSummary/>
  </React.StrictMode>,
)
